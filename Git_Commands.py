print("Helllo")

"""
cd .. parent folder
cd. child folder


In the Command Line (Linux/Windows)
cd (no arguments): Changes to your home directory, the default starting point for your user.
cd .: Stays in the current directory. It's generally redundant but confirms you're there.
cd ..: Moves you up one level to the parent directory.
cd /: Changes to the root directory (top of the file system). 

ls 

The git init command is used to create a new, empty Git repository.
git init  



git add .



Run

  git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

to set your account's default identity.
Omit --global to set the identity only in this repository.


git clone 
"""